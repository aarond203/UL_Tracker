<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".MainScreenActivity"
    tools:showIn="@layout/app_bar_main_screen">

    <ImageView
        android:id="@+id/ul_trans_logo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="32dp"
        android:layout_marginEnd="32dp"
        android:layout_marginStart="32dp"
        android:layout_marginTop="32dp"
        android:contentDescription="@string/opaque_background_logo"
        android:cropToPadding="false"
        app:srcCompat="@drawable/ul_crest_trns" />

    <TextView
        android:id="@+id/no_task"
        android:layout_width="149dp"
        android:layout_height="64dp"
        android:layout_centerInParent="true"
        android:text="No pending tasks"
        android:textAlignment="center"
        android:textColor="@android:color/black"
        android:textSize="25dp" />
</RelativeLayout>